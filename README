Before installing the REQUIREMENTS you will need to install the LDAP libraries

sudo apt-get install libldap2-dev libsasl2-dev

Usage
=====

Install the plugin either by setting PLUGINPATH in the panoptes config or by copying the directory to the plugins directory

Create a directory under <<dataset>>/pre e.g. <<dataset>>/pre/studies

In there create a settings file with the plugin settings values

When panoptes is run a data file will be created according to the settings which can be loaded in the usual way.


